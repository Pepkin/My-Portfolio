function requiredReading(pages, pagesPerHour, daysToDo){
    let hoursPerDay = pages / pagesPerHour
    let goal = hoursPerDay / daysToDo
    console.log(goal);

}
requiredReading(212,
    20 ,
    2 
    )
