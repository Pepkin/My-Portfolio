function printSquareMatrix(n) {
   let row = n
   let colums = n
   let rHolder = ""
   let cHolder = ""
   for(let i = 0; i < colums; i++){
        
        cHolder += `${n} `
            
        
        
   }   
   for(let j = 0; j < n; j++){
        console.log(cHolder);
   }
    
  }
  printSquareMatrix(7)